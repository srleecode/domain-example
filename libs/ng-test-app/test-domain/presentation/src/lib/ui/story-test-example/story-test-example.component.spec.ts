import { StoryTestExampleComponent } from './story-test-example.component';

describe('StoryTestExampleComponent', () => {
  let component: StoryTestExampleComponent;
  beforeEach(() => {
    component = new StoryTestExampleComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
