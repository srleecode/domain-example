import { UiStoryTestExampleComponent } from './story-test-example.component';

describe('UiStoryTestExampleComponent', () => {
  let component: UiStoryTestExampleComponent;
  beforeEach(() => {
    component = new UiStoryTestExampleComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
